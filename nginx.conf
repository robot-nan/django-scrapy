server {
    listen 80;
    servername 120.27.156.44 chinayunju.com;
    rewrite ^(.*) http://www.chinayunju.com/$1 permanent;
}
upstream superworm {
    server localhost:8000;
}
server {
    listen 80;
    server_name www.chinayunju.com;
    location /static/ {
        root /home/django-scrapy;
    }
    location / {
        try_files $request_uri @qfscu;
    }
    location @qfscu {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        if (!-f $request_filename){
           proxy_pass http://chinayunju;
           break;
            }
        proxy_intercept_errors on;
        proxy_redirect off;
        proxy_connnect_timeout 60;
    }
}
